{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "required": ["id", "key", "secret", "options"],
    "properties": {
      "id": {
        "type": "string",
        "minLength": 1,
        "description": "A unique identifier"
      },
      "key": {
        "type": "string",
        "minLength": 1,
        "pattern": "^[A-Za-z0-9]+$",
        "description": "API Key"
      },
      "secret": {
        "type": "string",
        "minLength": 1,
        "pattern": "^[A-Za-z0-9]+$",
        "description": "Secret Key (64 characters)"
      },
      "options": {
        "type": "object",
        "required": ["account_mode"],
        "description": "Optional configuration settings",
        "properties": {
          "account_mode": {
            "type": "string",
            "minLength": 1,
            "description": "Account mode identifier"
          },
          "test": {
            "type": "boolean",
            "description": "Enable test mode (optional)"
          }
        },
        "additionalProperties": false
      }
    },
    "additionalProperties": false
  }
  